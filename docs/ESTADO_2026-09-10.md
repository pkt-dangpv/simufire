# SimuFire — dónde lo dejamos

**Fecha:** 2026-09-10 · **HEAD:** `32c912e9` · **Subido a `origin/main`** · árbol limpio
**Suite:** `python scripts/check_product.py` → **46 OK** + 1 FAIL conocido
**Godot:** 4.7.1 · renderer `gl_compatibility`

Este documento es el punto de retomada. El plan completo, con el porqué de cada
tarea, sigue en `docs/ESTADO_Y_PLAN_2026-09-09.md`, que se ha ido marcando a
medida que se cerraban las fases.

---

## 1. Lo que se hizo en esta tanda (14 commits)

| fase | qué |
|---|---|
| **0** ✅ | Menú rehecho: fichas de escenario con resumen y panel «Retocar» (de 719 px a 542). G-3, sombra del sol. Los dos prompts al motor. |
| **1** ✅ | La planta llega a la vista: calle derivada de la planta, dos datos de plantas (total + actual, 0 = baja), tres tipologías de exterior por altura, fondo por manzana, ciudad de hormigón. |
| **2** 🟠 | Viento: mandos en editor y menú, dato hasta `BuildingModel`. **Falta la parte del motor.** |
| **3** ✅ | Mobiliario sin tocar mallas: orientación y parejas, cama de cabecero, colocación por grupos, catálogo del editor 14 → 38, piezas colgadas. |
| **6** 🟠 | G-5 cerrado: la llama mide lo que dice el HUD. |

---

## 2. El único FAIL de la suite

`Guardrail script unit tests` → `test_exit0_real_json`, que valida
`sim/validation/reports/reference_checks.json`. **Es de la línea del motor** (el
R2-1 de siempre, informes por regenerar) y ya fallaba antes de empezar. Todo lo
demás está en verde.

---

## 3. Lo que queda, y en qué orden lo propongo

### Bloqueado por la otra línea
- **N-5, parte motor**: corrección de la velocidad del viento por la altura de
  la apertura. Encargado en `docs/PROMPT_MOTOR_VIENTO_ALTURA.md`. Cuando llegue,
  aquí no hay que tocar nada.
- **N-2, patios interiores**: `docs/PROMPT_MOTOR_PATIO.md`. Fase 5.

### Se puede hacer ya
1. **Fase 4 — balcones (N-1)**. Se apoya en la fachada propia, el modelo de
   aperturas y las plantas totales, que ya están. **Necesita una decisión tuya**
   (ver §4).
2. **D-1 — el monolito del editor**: `editor/ScenarioEditor.gd` ronda las 8000
   líneas. No son funciones largas (solo 2 pasan de 100): son ~408 funciones y
   155 variables en una clase. Los dos bloques que piden módulo son «3D en vivo»
   y «objetos/mobiliario».
3. **D-3** la ficha de la sala la tapan los muebles · **D-4** el 3D en vivo se
   sale del fotograma a ~16 salas · **D-5** el doc del editor se quedó en el
   día 7 · **D-6** las sondas de captura no pasan por el camino real.

### Aparcado a conciencia (no es olvido)
- **El tinte del mueble en llamas** (rojo plano, sin tizne). Es material de
  mobiliario y el kit se cambia entero.
- **Las 7 plantillas sin `fuel_objects`**, que se amueblan con atrezo. Decisión
  tuya del 2026-09-06: se convierten cuando el motor esté terminado.
- **La bruma de distancia con la altura**, la otra mitad de N-3.
- **`NearNeighbour` no se construye nunca**: las piezas se generan y
  `_crosses_roadway` las descarta, porque desde EXT-1 la calle es un anillo y
  nuestro edificio es la manzana entera. O el anillo deja hueco para medianeras,
  o esas piezas sobran. Es una decisión de diseño del exterior.
- **Retornos de esquina y fila de atrás sin alzado**: probé a decorarlos y lo
  revertí porque no llegué a verlos en ninguna vista.

---

## 4. Decisiones tuyas pendientes

1. **Balcones (N-1)**: ¿solo en el edificio del jugador, o también en las plantas
   de arriba y abajo? Ahora que el edificio tiene plantas totales de verdad, la
   segunda opción ya significa algo: es lo que se ve al asomarse.
2. **El tope del viento**: implementado a **28 m/s** con la justificación de
   Beaufort 10 y el CTE. Si quieres otro, es una constante en `ui/WindRose.gd`.
3. **La convención de plantas**: implementada como la española —**0 = planta
   baja**, sin negativos—. Si prefieres que el 1 sea la baja, es una línea.

---

## 5. Herramientas nuevas de esta tanda

Todas headless salvo donde se diga. Las de captura necesitan ventana real.

| herramienta | para qué |
|---|---|
| `tools/probe_exterior_occlusion.gd` | Abanico de rayos desde la ventana: dice **el nombre del nodo** que encuentra cada uno. Es lo que desatascó el fondo gris. |
| `tools/probe_urban_typology.gd` | Tabla de las tres tipologías con su esbeltez. |
| `tools/probe_building_floors.gd` | Plantas, altura de planta y caída de la calle de los 10 escenarios. |
| `tools/probe_furniture_sense.gd` | Mide si el mobiliario tiene **sentido** (la tele mira al sofá, la cocina va seguida), no si cabe. |
| `tools/probe_fp_fire_scale.gd` | Mide **la malla de la llama construida**, no la fórmula. |
| `tools/validate_building_height.gd` | La planta llega a la vista y los vecinos la acompañan. |
| `tools/validate_exterior_occlusion.gd` | Por debajo del horizonte no se ve cielo; por encima, sí. |
| `tools/validate_wind_controls.gd` | El viento llega al motor y empuja donde debe. |
| `tools/validate_object_catalog.gd` | Todo arquetipo se puede colocar; la carga de fuego es creíble. |
| `--floor=` y `--total=` en `capture_visual_reference` | Capturar el mismo piso en otra planta o en un edificio más alto. |

---

## 6. Trampas que costaron tiempo y conviene no repetir

- **`godot --headless --import` reescribe `project.godot`** y borra
  `directional_shadow/size=4096` porque es el valor por defecto. No se pierde
  comportamiento, pero ensucia el diff: mirar `git diff project.godot` antes de
  commitear.
- **Una sonda mal calibrada inventa un problema.** Dije que la mesilla estaba en
  1/23 y monté medio arreglo encima; el número era de la sonda, que medía
  distancia entre centros con un tope fijo. Lo que hay que medir es el hueco
  entre las dos piezas. Las mesillas nunca estuvieron rotas.
- **Un `Array[Rect2]` descarta en silencio** lo que no sea un `Rect2`, y
  `blockers.duplicate()` conserva el tipo. Síntoma: la sala se colocaba sin
  detectar ningún choque.
- **Medir el mundo FP**: sin `fp.set_active(true)` el `_physics_process` sale por
  la puerta de atrás; y `mesh.visible` no basta, hace falta
  `is_visible_in_tree()` o se cuelan las mallas de las salas ocultas.
- **`_try_side` deja la pieza en la última pose que probó aunque devuelva
  `false`**: hay que guardarse la pose entera antes, no solo el centro.
